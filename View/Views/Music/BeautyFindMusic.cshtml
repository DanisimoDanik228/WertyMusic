@model IEnumerable<Domain.Models.Music>

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Поиск песен</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #f0f0f0;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid rgba(255,255,255,0.1);
        }

        .page-title {
            font-size: 2.4rem;
            font-weight: 700;
            background: linear-gradient(to right, #00b4db, #0083b0);
            -webkit-background-clip: text;
            color: transparent;
        }

        .nav-links a {
            margin-right: 15px;
            text-decoration: none;
            color: #8dd6f9;
            padding: 8px 16px;
            background: rgba(0, 131, 176, 0.15);
            border-radius: 8px;
            transition: 0.3s;
        }

        .nav-links a:hover {
            background: rgba(0, 131, 176, 0.3);
        }

        .search-box {
            margin: 30px 0;
            background: rgba(25, 40, 50, 0.7);
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .search-form {
            display: flex;
            gap: 15px;
        }

        .search-form input {
            flex: 1;
            padding: 12px 15px;
            border-radius: 8px;
            border: none;
            outline: none;
            font-size: 1rem;
            background: rgba(255,255,255,0.9);
            color: #333;
        }

        .search-form button {
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            background: linear-gradient(to right, #00b4db, #0083b0);
            color: white;
            transition: 0.3s;
        }

        .search-form button:hover {
            opacity: 0.9;
            transform: translateY(-2px);
        }

        .results-info {
            margin: 25px 0;
            font-size: 1.2rem;
            color: #8dd6f9;
            display: none; /* Скрыто до начала поиска */
        }

        .music-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            background: rgba(25, 40, 50, 0.7);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            display: none; /* Скрыта до появления первых результатов */
        }

        .music-table thead {
            background: linear-gradient(to right, #00b4db, #0083b0);
        }

        .music-table th,
        .music-table td {
            padding: 16px;
            text-align: left;
        }

        .music-table th {
            color: white;
            text-transform: uppercase;
            font-size: 0.95rem;
        }

        .music-table tbody tr {
            transition: 0.3s;
            animation: fadeIn 0.4s ease-out forwards;
        }

        .music-table tbody tr:hover {
            background: rgba(0,180,219,0.1);
        }

        .music-name {
            font-weight: 600;
            color: white;
        }

        .artist-name {
            color: #8dd6f9;
        }

        .source-name {
            color: #a0e7a0;
        }

        .download-link {
            color: #ffb347;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s ease;
            display: inline-block;
            padding: 5px 10px;
            border-radius: 4px;
            background: rgba(255, 179, 71, 0.1);
        }

        .download-link:hover {
            color: #ff8c00;
            background: rgba(255, 179, 71, 0.2);
            text-decoration: underline;
        }

        .download-all {
            margin-top: 25px;
            text-align: right;
            display: none;
        }

        .download-all button {
            padding: 12px 20px;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            background: linear-gradient(to right, #ffb347, #ff8c00);
            color: #2c2c2c;
            transition: 0.3s;
        }

        .status-badge {
            font-size: 0.9rem;
            margin-left: 15px;
            padding: 4px 10px;
            border-radius: 20px;
            background: rgba(141, 214, 249, 0.1);
        }

        @@keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Spinner for loading */
        .spinner {
            display: inline-block;
            width: 14px;
            height: 14px;
            border: 2px solid rgba(141, 214, 249, 0.3);
            border-radius: 50%;
            border-top-color: #8dd6f9;
            animation: spin 1s linear infinite;
            margin-right: 8px;
        }

        @@keyframes spin {
            to { transform: rotate(360deg); }
        }
    </style>
</head>

<body>
<div class="container">

    <div class="header">
        <h1 class="page-title">Поиск песен</h1>
        <div class="nav-links">
            <a href="/">← На главную</a>
            <a href="/Music/AllMusics">📀 Все песни</a>
        </div>
    </div>

    <div class="search-box">
        <div class="search-form">
            <input id="searchInput" placeholder="Введите название песни или исполнителя..." />
            <button id="btnSearch">🔍 Найти</button>
        </div>
    </div>

    <!-- Информационный блок (показывается при поиске) -->
    <div id="resultsInfo" class="results-info">
        Найдено песен: <strong id="totalCount">0</strong>
        <span id="loadingStatus" class="status-badge">
            <span class="spinner"></span> Поиск продолжается...
        </span>
    </div>

    <!-- Таблица результатов -->
    <table id="musicTable" class="music-table">
        <thead>
        <tr>
            <th>Название</th>
            <th>Исполнитель</th>
            <th>Ресурс</th>
            <th>Ссылка на скачивание</th>
        </tr>
        </thead>
        <tbody id="musicTableBody">
            <!-- Элементы добавляются через JS -->
        </tbody>
    </table>

    <!-- Кнопка Скачать всё -->
    <div id="downloadAllSection" class="download-all">
        <form asp-action="DownloadZip" method="post">
            <input type="hidden" name="connectionId" id="hiddenConnectionId" />
            <button type="submit">
                📥 Скачать все (<span id="btnCount">0</span>)
            </button>
        </form>
    </div>

</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/6.0.1/signalr.min.js"></script>

<script>
    const connection = new signalR.HubConnectionBuilder()
        .withUrl("/musicHub")
        .build();

    let musicCount = 0;

    // Прием данных
    connection.on("ReceiveMusic", function (song) {
        musicCount++;
        
        // Показываем элементы интерфейса
        document.getElementById("resultsInfo").style.display = "block";
        document.getElementById("musicTable").style.display = "table";
        document.getElementById("downloadAllSection").style.display = "block";

        // Обновляем счетчики
        document.getElementById("totalCount").innerText = musicCount;
        document.getElementById("btnCount").innerText = musicCount;

        // Рендерим строку (в стиле вашего оригинала)
        const tbody = document.getElementById("musicTableBody");
        const row = `
            <tr>
                <td class="music-name">${song.musicName}</td>
                <td class="artist-name">${song.artistName}</td>
                <td class="source-name">${song.siteSource}</td>
                <td>
                    <a href="${song.downloadUrl}" class="download-link" target="_blank" rel="noopener noreferrer">Скачать</a>
                </td>
            </tr>`;
        
        tbody.insertAdjacentHTML('beforeend', row);
    });

    // Завершение поиска
    connection.on("SearchFinished", function () {
        document.getElementById("loadingStatus").innerHTML = "✅ Поиск завершен";
    });

    // Ошибка
    connection.on("ReceiveError", function (message) {
        alert("Ошибка: " + message);
        document.getElementById("loadingStatus").innerHTML = "❌ Ошибка при поиске";
    });

    // Старт SignalR
    connection.start().then(function () {
        console.log("SignalR Connected");
        document.getElementById("hiddenConnectionId").value = connection.connectionId;
    }).catch(err => console.error(err.toString()));

    // Логика кнопки Найти
    document.getElementById("btnSearch").addEventListener("click", function () {
        const query = document.getElementById("searchInput").value;
        if (!query) return alert("Введите запрос");

        const connectionId = connection.connectionId;
        if (!connectionId) return alert("Соединение устанавливается, подождите...");

        // Сброс интерфейса
        musicCount = 0;
        document.getElementById("musicTableBody").innerHTML = "";
        document.getElementById("resultsInfo").style.display = "block";
        document.getElementById("loadingStatus").innerHTML = '<span class="spinner"></span> Поиск продолжается...';
        document.getElementById("downloadAllSection").style.display = "none";
        document.getElementById("musicTable").style.display = "none";

        const payload = {
            MusicName: query,
            ConnectionId: connectionId
        };
        
        fetch('/Music/FindMusic', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(payload)
        })
        .then(response => {
            if (!response.ok) throw new Error("Ошибка запуска поиска");
        })
        .catch(err => alert(err.message));
    });
</script>

</body>
</html>